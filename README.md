# file-reader-by-chunk
this project to read the file in chunks from Disk instead loading all file into memory

we can start the server using lite-server and it will load the index.html file into browser
